name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # package.json の packageManager (pnpm@11.25.0) を読んで同じ版を入れる
      - name: pnpm を用意する
        uses: pnpm/action-setup@v6

      - name: Node を用意する
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - name: 依存をインストールする
        run: pnpm install --frozen-lockfile

      # packages/core の tsconfig は tests を include しているため、
      # マスタデータの不変性を守る @ts-expect-error はこのステップでのみ検証される。
      # test だけでは通ってしまうので、typecheck を落とさないこと。
      - name: 型を検査する
        run: pnpm -r typecheck

      # apps/worker は wrangler.toml の [assets] で apps/web/dist を指しており、
      # このディレクトリが無いと Worker のテストは1件も起動しない
      # （vitest-pool-workers が設定読み込みの時点で落ちる）。
      # dist は生成物なのでリポジトリに入っていない。先にビルドする。
      - name: 画面をビルドする
        run: pnpm --filter @mq/web build

      - name: テストを走らせる
        run: pnpm -r test
