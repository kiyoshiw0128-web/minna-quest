{
  "name": "minna-quest",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "release": "pnpm --filter @mq/web build && pnpm --filter @mq/worker exec sh scripts/check-migrations.sh && pnpm --filter @mq/worker run deploy"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@11.25.0"
}
