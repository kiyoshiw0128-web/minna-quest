{
  "name": "minna-quest",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "release": "pnpm --filter @mq/web build && pnpm --filter @mq/worker exec wrangler d1 migrations apply minna-quest --remote && pnpm --filter @mq/worker deploy"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@11.25.0"
}
